name: Scheduled Greetings
on:
    schedule:
        - cron: '*/2 * * * *'

jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: Echo-current Time
              run: echo "The current server time is $(date)"

